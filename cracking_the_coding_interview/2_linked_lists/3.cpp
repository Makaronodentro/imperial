/* 2.3 | Delete middle node
 * -----------------------------------------------------------------------------
 * Desc  : Delete middle node, given access only to that node. Singly
 *
 * Notes : Set middle->x = middle->next->x, middle = middle->next->next, delete
 */
